import React from "react";

export interface IconProps {
  name: string;
  size?: number;
  color?: string;
  className?: string;
  style?: React.CSSProperties;
  ariaLabel?: string;
  fill?: string;
  stroke?: string;
  width?: number;
}
